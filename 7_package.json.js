//package.json

/*   1. npm init (node package module) --initialising package.json
      
     2 .creating package of json
            npm i colors
    
     3 . Using that package.
  */

const colors = require("colors");

console.log("ram".red);